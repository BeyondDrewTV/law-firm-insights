import { useId, useState, type ReactNode } from "react"; import { ChevronDown } from "lucide-react"; interface ShowDetailsProps { title: string; summary: ReactNode; children: ReactNode; defaultOpen?: boolean; } const ShowDetails = ({ title, summary, children, defaultOpen = false }: ShowDetailsProps) => { const [open, setOpen] = useState(defaultOpen); const detailsId = useId(); return ( <section className="gov-level-2 rounded-xl p-5"> <h3 className="text-base font-semibold text-foreground">{title}</h3> <div className="mt-2 text-sm text-muted-foreground">{summary}</div> <button type="button" aria-expanded={open} aria-controls={detailsId} className="mt-4 inline-flex items-center gap-1.5 text-sm font-medium text-primary transition-colors hover:text-primary/80 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2" onClick={() => setOpen((previous) => !previous)} > {open ? "Hide details" : "Show details"} <ChevronDown size={16} className={`transition-transform ${open ? "rotate-180" : ""}`} /> </button> {open && ( <div id={detailsId} className="mt-4 border-t border-border pt-4 text-sm leading-relaxed text-muted-foreground"> {children} </div> )} </section> ); }; export default ShowDetails; 
