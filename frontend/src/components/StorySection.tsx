import { Link } from "react-router-dom"; import { ArrowRight, BarChart3, BriefcaseBusiness, Upload } from "lucide-react"; import MarketingDashboardPreview from "@/components/MarketingDashboardPreview"; import { useAuth } from "@/contexts/AuthContext"; import { coreAudience, coreNarrative } from "@/content/marketingCopy"; const personas = [ { icon: BriefcaseBusiness, title: "Managing Partners", detail: "See at-risk matters and clear actions before leadership meetings.", }, { icon: BarChart3, title: "Operations and Admin", detail: "Run recurring report cycles without adding manual spreadsheet work.", }, { icon: Upload, title: "Intake and Marketing", detail: "Find recurring client friction in intake, updates, and communication.", }, ]; const StorySection = () => { const { isLoggedIn, isLoading } = useAuth(); return ( <section className="section-padding border-y border-border bg-gradient-to-b from-secondary/30 to-background"> <div className="section-container"> <div className="text-center mb-12 reveal"> <span className="gov-badge gov-badge-watch mb-4 inline-block">Who It Is For</span> <h2 className="section-heading">Built for firm leaders who need clear direction</h2> <p className="section-subheading mx-auto"> {coreNarrative} {coreAudience} </p> </div> <div className="grid gap-4 md:grid-cols-3"> {personas.map((persona, idx) => ( <article key={persona.title} className="gov-level-2 reveal" style={{ transitionDelay: `${idx * 100}ms` }} > <div className="inline-flex items-center justify-center w-9 h-9 rounded-md bg-primary/10 border border-border mb-4"> <persona.icon size={16} className="text-primary" /> </div> <h3 className="text-base font-semibold text-foreground mb-1">{persona.title}</h3> <p className="text-sm text-muted-foreground">{persona.detail}</p> </article> ))} </div> <div className="mt-8 reveal"> <MarketingDashboardPreview compact variant="operations" /> <p className="mt-2 text-xs text-muted-foreground"> Dashboard view: trend shifts, sentiment mix, and priority queue for immediate follow-up. </p> </div> <div className="mt-10 reveal flex flex-wrap items-center justify-center gap-4"> {!isLoading && isLoggedIn ? ( <Link to="/dashboard" className="gov-btn-primary"> Go to Dashboard <ArrowRight size={16} className="ml-2" /> </Link> ) : ( <Link to="/signup" className="gov-btn-primary"> Start Free <ArrowRight size={16} className="ml-2" /> </Link> )} <Link to="/how-it-works#reporting-loop" className="gov-btn-secondary"> See how it works </Link> </div> </div> </section> ); }; export default StorySection; 

